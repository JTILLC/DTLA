# Favicon Files

Place your favicon files in this `public` directory:

- favicon.ico
- favicon.svg
- favicon-96x96.png
- apple-touch-icon.png

These files will be served from the root of your deployed site.

The `site.webmanifest` file is already configured and ready to use.
